# Runtime integration — generated by agentic-control-kernel
# Runtime: subprocess | Ledger: file

runtime:
  kind: subprocess
  # default subprocess — no extra config

ledger:
  kind: file
  # default file ledger — writes to ledger.jsonl

